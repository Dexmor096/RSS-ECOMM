export type LoginInputs = {
  email: string;
  password: string;
};

export interface RegistrationInputs {
  email: string;
  name: string;
  lastname: string;
  password: string;
  address: string;
}
