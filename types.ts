export interface ButtonMain {
  text: string;
  color: string;
  border: string;
  margin: string;
}

export interface SectionTitleStyles {
  mt?: string;
  title: string;
  color: string;
  width: number;
}
